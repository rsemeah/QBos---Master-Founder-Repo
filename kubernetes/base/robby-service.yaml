apiVersion: v1
kind: Service
metadata:
  name: robby
  namespace: quietbuild
  labels:
    app: robby
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: robby
