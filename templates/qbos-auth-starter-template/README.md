# QuietBuild OS - Auth Starter Template

This is an authentication-ready Next.js application template with Supabase Auth integration, created by QuietBuild OS.

## Features

- ✅ Next.js 14 with App Router
- ✅ Supabase Authentication (Email/Password)
- ✅ TypeScript
- ✅ Tailwind CSS
- ✅ Protected routes with middleware
- ✅ Login and Signup pages
- ✅ Dashboard for authenticated users

## Environment Variables

Create a `.env.local` file with the following variables:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

## Getting Started

1. Install dependencies:

```bash
npm install
```

2. Set up your environment variables in `.env.local`

3. Run the development server:

```bash
npm run dev
```

4. Open [http://localhost:3000](http://localhost:3000)

## Project Structure

```
├── app/
│   ├── api/
│   │   └── health/          # Health check endpoint
│   ├── login/               # Login page
│   ├── signup/              # Signup page
│   ├── dashboard/           # Protected dashboard
│   └── page.tsx             # Landing page
├── middleware.ts            # Route protection
└── package.json
```

## Deployment

This template is ready to deploy on Vercel:

```bash
vercel
```

## Built by QuietBuild OS

This app was generated by [QuietBuild OS](https://quietbuild.com) - AI-powered app generation.
